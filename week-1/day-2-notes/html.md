HTML = HYPERTEXT MARKUP LANGUAGE.
HYPERTEXT = HYPERTEXT CAN CONNECT OTHER PAGES THROUGH LINKS.
Markup = markup means adding meaning around content.
web = core ideas of web is pages are connected through links.  
Brower = its read html as a tree. brower always reads html at first.
when brower reads html its creats a DOM.
DOM = Document Object Model. This is tree version of html.
 Tag = <h1>
 Element = opening tag + content + closing tag = <h1>hello</h1> 
 Attributes sits at the opening tag <a href="http://www.gogle.com">google</a>